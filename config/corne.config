# Enable deep sleep
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Force USB output over Bluetooth when plugged in
CONFIG_ZMK_USB_SELECT_BY_DEFAULT=y

# Uncomment the line below ONLY if plugging into the RIGHT side works but the left doesn't
# CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n